# --- Accès données -----------------------------------------------------------
psycopg2-binary>=2.9
SQLAlchemy>=2.0
supabase>=2.0
python-dotenv>=1.0

# --- Manipulation / calcul ----------------------------------------------------
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
PyYAML>=6.0

# --- Modélisation -------------------------------------------------------------
scikit-learn>=1.4  # aussi utilisé pour les métriques du classifieur hurdle (PR-AUC, Brier, log loss)
lightgbm>=4.3
catboost>=1.2
xgboost>=3.0  # ranking (XGBRanker) pour src.recsys_v4.models
statsforecast>=1.7
mlforecast>=0.13
utilsforecast>=0.2
optuna>=3.6
holidays>=0.50
statsmodels>=0.14  # panel à effets fixes (pricing), erreurs clusterisées par produit

# --- Visualisation / restitution ---------------------------------------------
matplotlib>=3.8
plotly>=5.20
dash>=2.17
dash-bootstrap-components>=1.6
XlsxWriter>=3.2

# --- Service applicatif (API V4) ----------------------------------------------
# Necessaires au demarrage de `uvicorn api_v4.main:app`. Sans elles, la commande
# de build `pip install -r requirements.txt` produit un environnement ou le
# serveur ne peut pas demarrer.
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
joblib>=1.4

# --- Qualité / tests ----------------------------------------------------------
pytest>=8.0
tabulate>=0.9
